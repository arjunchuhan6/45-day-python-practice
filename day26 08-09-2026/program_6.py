# Python Program 6: Install from requirements.txt

# Activate the virtual environment first:
# .\.venv\Scripts\Activate.ps1

# Install every package listed in requirements.txt:
# python -m pip install -r requirements.txt

# Explanation:
# install tells pip to install packages.
# -r means read package names from a requirements file.
# requirements.txt contains the packages and their required versions.

# Pip reads the file and installs all listed dependencies automatically.

print("Program 6: Install packages with 'python -m pip install -r requirements.txt'")
