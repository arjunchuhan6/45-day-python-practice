# Python Program 4: Check an installed package

# Make sure the virtual environment is active first:
# .\.venv\Scripts\Activate.ps1

# Check information about the requests package:
# python -m pip show requests

# The command displays the package name, version, installation location,
# summary, homepage, and the packages it depends on.

# To see every package installed in the active environment, use:
# python -m pip list

print("Program 4: Check an installed package with 'python -m pip show requests'")
